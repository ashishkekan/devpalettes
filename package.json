{
  "name": "devpalettes-static",
  "private": true,
  "description": "Build tooling for Devpalettes static site (Tailwind CSS compile only).",
  "scripts": {
    "build:tailwind": "tailwindcss -c tailwind.config.js -i ./src/tailwind.css -o ./css/tailwind.min.css --minify"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.41",
    "tailwindcss": "^3.4.17"
  }
}

